{
  "name": "agent-tools",
  "version": "1.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node build.js",
    "build:no-scripts": "node build.js --no-scripts",
    "build:doc-testing": "cd src/skills/doc-testing/scripts/src && npm run build",
    "build:inline": "echo 'No source directory for inline-test-injection; rebuild not available'",
    "test:doc-testing": "cd skills/doc-testing/scripts && ./test-skill.sh",
    "test:inline": "cd skills/inline-test-injection/scripts && ./test-skill.sh",
    "prepare": "husky",
    "validate:skills": "./scripts/validate-skills.sh"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
